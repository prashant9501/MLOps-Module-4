# FreshBasket Docker Compose Environment Variables
# Copy to .env and update with your values:
#   cp .env.example .env

# PostgreSQL password (used by db, dashboard, and mlflow services)
DB_PASSWORD=freshbasket2026

# Set to "true" to run the Streamlit dashboard with synthetic data
# (no real model artifacts or RDS connection needed)
DEMO_MODE=true
